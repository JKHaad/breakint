{
  "name": "breakint-contact-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Contact form backend for breakint.com — receives visitor submissions and relays them via SMTP to info@breakint.com",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "nodemailer": "^9.0.3"
  }
}
